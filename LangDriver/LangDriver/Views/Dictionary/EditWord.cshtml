@model LangDriver.Common.Models.Word

@{
    ViewData["Title"] = "EditWord";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>EditWord</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditWord">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WordRus" class="control-label"></label>
                <input asp-for="WordRus" class="form-control" />
                <span asp-validation-for="WordRus" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WordEng" class="control-label"></label>
                <input asp-for="WordEng" class="form-control" />
                <span asp-validation-for="WordEng" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Update" class="btn btn-default" />
                <input type="button" value="Cancel" class="btn btn-default" onclick="location.href='@Url.Action("GetDictionary", "Dictionary")'"/>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="GetDictionary">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
